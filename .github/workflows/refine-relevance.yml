name: Archived refine-relevance snapshot action
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This historical importer is archived. Edit canonical JSON and run the read-only validators instead."
